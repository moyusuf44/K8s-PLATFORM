pipeline {
    agent any 
    
     environment {
        AWS_REGION = 'eu-north-1'
    }

    stages{
        stage('terraform-state-locking'){
            steps {
                withCredentials([[
                $class: 'AmazonWebServicesCredentialsBinding',
                credentialsId: 'aws-credentials'
                ]]) {
                dir ('Infra-state-lock'){
                bat 'terraform init'
                bat 'terraform apply --auto-approve'
                    }
                }
            }
        }
    }
}